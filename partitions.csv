# CardSat partition layout -- Cardputer ADV (ESP32-S3FN8, 8 MB flash).
#
# WHY THIS EXISTS. The stock "huge_app" scheme is designed for a 4 MB part: 3 MB app,
# 896 KB filesystem, 4 MB of an 8 MB chip simply unused. CardSat reached 96.9% of that
# 3 MB app (97 KB free), which is not enough headroom for the BASIC and calculator work
# and leaves no margin for anything unforeseen.
#
# BUDGET, and why it is not larger. Most users install through Launcher
# (bmorcelli/Launcher), which lives in flash alongside the app and writes its own
# partition table sized to the binary it is installing. So the app cannot simply expand
# to fill the chip: Launcher itself, plus its data, has to fit too. 4 MB app + 1.5 MB
# filesystem + overhead = ~5.9 MB, leaving ~2.4 MB for Launcher and its storage on an
# 8 MB part. That is a deliberate ceiling, not an accident -- raising the app further
# would start squeezing the tool most people use to install it.
#
# TWO THINGS THAT MUST NOT CHANGE:
#   * The filesystem partition MUST stay named "spiffs" with subtype spiffs. CardSat
#     calls LittleFS.begin(true), which looks up that label by default; renaming it to
#     "littlefs" mounts nothing and silently loses every setting, note and favourite.
#   * The coredump partition MUST stay. CardSat reads the panic backtrace back on the
#     next boot (esp_core_dump.h in app.cpp); without it a crash leaves no evidence.
#
# 1.5 MB of LittleFS is for operators running without a microSD -- settings, notes,
# favourites, logs and cached elements all live there. That is ~1.7x the old 896 KB.
#
# Offsets are contiguous and must stay so: app0 begins at 0x10000 because the bootloader
# occupies the space below it.
#
# Name,   Type, SubType, Offset,   Size,      Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x400000,
spiffs,   data, spiffs,  0x410000, 0x180000,
coredump, data, coredump,0x590000, 0x10000,
